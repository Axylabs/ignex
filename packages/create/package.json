{
  "name": "create-ignex",
  "version": "0.1.16",
  "description": "Scaffold a new Ignex app — `npm create ignex` / `bun create ignex`",
  "type": "module",
  "license": "MIT",
  "bin": {
    "create-ignex": "./bin/create-ignex.js"
  },
  "files": [
    "bin"
  ],
  "engines": {
    "bun": ">=1.4"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p ../../tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@ignex/cli": "workspace:*"
  }
}
