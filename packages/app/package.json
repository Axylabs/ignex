{
  "name": "@ignex/app",
  "version": "0.1.7",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun builder.ts",
    "compile": "bun builder.ts --compile",
    "smoke": "bun dist/__server.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p ../../tsconfig.json"
  },
  "dependencies": {
    "@ignex/core": "workspace:*",
    "@ignex/cli": "workspace:*",
    "@ignex/ninox": "file:../../../ignex-mongodb",
    "mongodb": "^7.5.0",
    "typebox": "^1.3.15"
  },
  "devDependencies": {
    "@ignex/compiler": "workspace:*",
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  },
  "engines": {
    "bun": ">=1.4"
  }
}
