/**
 * @fileoverview `lifecycle` domain — request pipeline & composition.
 *
 * Modules: derive, guard, hooks, lifecycle, macro, plugin. Re-exported here
 * for internal and subpath consumers; the top-level `index.ts` is the
 * canonical public surface.
 */
export * from "./derive";
export * from "./guard";
export * from "./hooks";
export * from "./lifecycle";
export * from "./macro";
export * from "./plugin";
