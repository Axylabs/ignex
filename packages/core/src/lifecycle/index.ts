/**
 * @fileoverview `lifecycle` domain — request pipeline & composition.
 *
 * Modules: hooks, lifecycle, plugin. Re-exported here for internal and
 * subpath consumers; the top-level `index.ts` is the canonical public
 * surface.
 */
export * from "./hooks";
export * from "./lifecycle";
export * from "./plugin";
