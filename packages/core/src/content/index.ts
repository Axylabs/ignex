/**
 * @fileoverview `content` domain — rendering & localization.
 *
 * Modules: i18n, template. Re-exported here for internal and subpath
 * consumers; the top-level `index.ts` is the canonical public surface.
 */
export * from "./i18n";
export * from "./template";
